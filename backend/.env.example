# Server configuration
HOST=127.0.0.1
PORT=8000

# CORS settings (frontend origin)
FRONTEND_ORIGIN=http://localhost:5173

# Path to the trained ML model (relative to backend/ directory or absolute path)
MODEL_PATH=../models/crop_yield_model.pkl
